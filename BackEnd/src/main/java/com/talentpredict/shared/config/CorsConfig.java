package com.talentpredict.shared.config;

// CORS is configured centrally in SecurityConfig.corsConfigurationSource()
// This class is intentionally left empty to avoid duplicate/conflicting CORS configs.
